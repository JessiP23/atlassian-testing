# secrets — never tracked
.env*
*.pem

# installs
node_modules/

# per-run output (durable copy lives in S3: runs/, evidence/, metrics/)
ci-runs/
agent/graph/runs/
agent/graph/ci-runs/
agent/.par/
agent/.baseline/

# Claude Code runtime state (staged only in CI)
.claude/

.DS_Store
